# API and credential guide

Local Article normalization, Evidence preparation, review, and evaluation do
not require API credentials. Publisher acquisition and model execution are
separately guarded by CLI execution flags.

Store credentials in `.env` using the variable names shown by
`comproscanner sources`. The file is ignored by Git. Never place a key in a
preset, command example, prediction JSON, or run manifest.
