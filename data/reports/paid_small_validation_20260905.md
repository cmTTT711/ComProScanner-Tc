# 少量真实付费实验（2026-09-05）

已结束：3 个输入，5 次模型 HTTP 请求，全部返回 HTTP 200，没有追加付费重试，没有恢复 30 篇批跑。

| 实验 | 模型/次数 | 结果 |
|---|---|---|
| 第 1 篇论文中含 1103 K 的一条真实文本 Evidence，走旧 identifier/extractor/formatter/清洗/JSON/review | Qwen Flash 1 次、DeepSeek V4 Flash 2 次 | 输出 BiFeO3 / 1103 K；与该篇已有 Gold 条目完全匹配，限定样本 TP=1、FP=0、FN=0 |
| 同篇论文真实 Fig. 6，直接测试图片工具 | Qwen VL Plus 1 次 | 调用成功、JSON 合法，但读图准确性未通过：例如 x=0.0 主图峰位目测接近 700 K，模型给出 650 K。其余多组峰位也需核查，未纳入 Tc 预测 |
| 单相 Ba1-xSrxTiO3 控制输入，直接测试公式工具 | DeepSeek V4 Flash 1 次 | 返回 (Ba(1-x)Sr(x))TiO3，通式正确；输入明确为控制用例，不冒充论文结论 |

图中请求的是 10 kHz 介电常数峰值温度，不把峰位自动认定为 Curie temperature。图像/公式是直接工具调用测试，并非其完整 Evidence→抽取链路的在线验收。未增加表格样本，未单独验证材料恢复 HTTP 服务。

## 用量

- 已知图片工具：输入 440、输出 212，共 652 tokens。
- 已知公式工具：输入 1380、输出 330（其中 reasoning 318），共 1710 tokens。
- 已知两次工具合计 2362 tokens。**这不是全部 5 次调用的总量**。
- 文本链路 3 次真实请求成功，但 CrewAI 上报全零 counters；实际消耗未知，不应解释为免费。已将后续运行的全零计数标为缺失，不为补统计重新付费调用。
- 未获取供应商账单，费用未估算。每次请求输出上限 2048 tokens，实验硬上限 8 次模型 HTTP 请求，实际使用 5 次。

## 结论

文本的小样本真实链路通过，公式工具控制用例通过。图片工具只能确认接口可用，**不能认为抽取准确性已达生产要求**。因此仍不适合宣布全项目无错误或开始依赖读图结果的无人值守生产。

原始请求计数、工具结果和用量保存在 `outputs/runs/paid_small_validation_20260905/paid_validation.json`；逐 Evidence JSON、原始 Crew 响应和 review.xlsx 位于同一目录。Gold 原件未修改。本次已停止，无后台继续付费任务。
